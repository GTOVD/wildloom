{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wildloom.dev/schemas/species-catalog.schema.json",
  "title": "Wildloom species catalog",
  "type": "object",
  "required": ["schema_version", "species_count", "species"],
  "properties": {
    "$schema": { "type": "string" },
    "schema_version": { "type": "string" },
    "content_note": { "type": "string" },
    "species_count": { "type": "integer", "minimum": 1 },
    "species": {
      "type": "array",
      "items": { "$ref": "#/$defs/speciesLine" }
    }
  },
  "$defs": {
    "affinity": {
      "type": "string",
      "enum": [
        "thermal",
        "cryo",
        "aqueous",
        "galvanic",
        "mineral",
        "flora",
        "aero",
        "luminous",
        "void",
        "sonic",
        "corrosive",
        "plasmic"
      ]
    },
    "stage": {
      "type": "object",
      "required": ["stage", "code", "name"],
      "properties": {
        "stage": { "type": "integer", "minimum": 1, "maximum": 3 },
        "code": { "type": "string", "enum": ["morph", "ascension", "zenith"] },
        "name": { "type": "string", "minLength": 1 }
      }
    },
    "speciesLine": {
      "type": "object",
      "required": ["id", "catalog_number", "name", "stages", "habitat", "blurb", "species_tags"],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9_]+$" },
        "catalog_number": { "type": "integer", "minimum": 1 },
        "name": { "type": "string", "minLength": 1 },
        "stages": {
          "type": "array",
          "minItems": 3,
          "maxItems": 3,
          "items": { "$ref": "#/$defs/stage" }
        },
        "primary_affinity": {
          "description": "Optional dex/UI collapsed chip — omit or null = no catalog-level typing (authoritative affinities live on creature instances only).",
          "oneOf": [{ "$ref": "#/$defs/affinity" }, { "type": "null" }]
        },
        "secondary_affinity": {
          "description": "Optional second dex chip when mirroring instance summaries into catalog tooling.",
          "oneOf": [{ "$ref": "#/$defs/affinity" }, { "type": "null" }]
        },
        "affinity_emphasis_hint": {
          "description": "Optional seed vector for procedural naming/tutorials — never authoritative for combat.",
          "type": "object",
          "additionalProperties": { "type": "number", "minimum": 0, "maximum": 1 }
        },
        "habitat": { "type": "string" },
        "blurb": { "type": "string" },
        "species_tags": { "type": "array", "items": { "type": "string" } }
      }
    }
  }
}
