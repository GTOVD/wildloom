{
  "name": "wildloom",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "gen:species": "node scripts/gen-species-catalog.mjs"
  }
}
